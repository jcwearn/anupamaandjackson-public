name: Refresh schedule index

# Rebuilds public/schedule-index.json from the With Joy export and commits it
# when the roster actually changed. Pushing to main triggers the Cloudflare
# Pages deploy, so no separate deploy step is needed here.
#
# Timing: the exporter writes the sheet daily at 06:00 America/New_York (see
# k3s-cluster/apps/withjoy-exporter/cronjob.yaml), and this has to run after it.
# GitHub Actions cron is UTC-only — there is no timeZone field — so 11:30 UTC is
# the earliest slot that stays after the export year-round:
#
#   EST (winter, UTC-5): 11:30 UTC = 06:30 ET, 30 minutes after
#   EDT (summer, UTC-4): 11:30 UTC = 07:30 ET, 90 minutes after
#
# The hour of drift across DST is deliberate. Firing early is the failure that
# matters: it rebuilds the index from the previous day's sheet, silently. If the
# exporter's schedule moves, move this with it.

on:
  schedule:
    - cron: '30 11 * * *'
  workflow_dispatch:

permissions:
  contents: write

# Never let a scheduled run and a manual run push at the same time.
concurrency:
  group: refresh-schedule-index

jobs:
  refresh:
    outputs:
      # Whether this run actually committed. The deploy job below needs it,
      # because a push made with the default GITHUB_TOKEN does NOT trigger
      # `on: push` workflows -- GitHub blocks that to prevent recursion -- so
      # deploy.yml never sees this commit and the published schedule would
      # silently stop updating while this job reported success every morning.
      changed: ${{ steps.commit.outputs.changed }}
      # The commit this run pushed. The deploy job cannot work it out for
      # itself: github.sha is the head this RUN started at, which is the
      # commit BEFORE the one below. See the comment on `deploy:`.
      sha: ${{ steps.commit.outputs.sha }}
    runs-on: ubuntu-latest
    steps:
      # fetch-depth 0 rather than checkout's default of 1: the push below
      # rebases when main has moved, and a one-commit repo has nothing to
      # rebase onto.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0

      # Same .node-version as CI and the Pages build. The generator runs
      # against Node builtins only, so the exact patch matters less here than
      # it does in CI -- but a nightly job that commits to main should not be
      # the one place running a different Node.
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: '.node-version'

      # No `npm ci`: the generator deliberately uses only Node builtins, so
      # this job needs nothing from node_modules.
      - name: Build encrypted index
        env:
          GOOGLE_SA_EMAIL: ${{ secrets.GOOGLE_SA_EMAIL }}
          GOOGLE_SA_PRIVATE_KEY: ${{ secrets.GOOGLE_SA_PRIVATE_KEY }}
          SHEET_ID: ${{ secrets.SHEET_ID }}
          # The only key to the /guest-summary payload. The generator refuses to
          # run without it rather than publishing an index that looks fine and
          # is keyed to nothing -- so an unset secret fails this job loudly,
          # which is the point.
          ADMIN_PASSPHRASE: ${{ secrets.ADMIN_PASSPHRASE }}
        run: node scripts/build-schedule-index.js

      # Commits land as me, SSH-signed, so a nightly refresh reads the same as
      # any other commit on main instead of an unsigned bot row.
      #
      # PUBLIC_SYNC_SIGNING_KEY is the account-wide signing key set up for the
      # public snapshot; publish.yaml passes the same secret through to
      # jcwearn/workflows. It is shared, despite the name -- renaming it means
      # changing both workflows.
      - name: Commit if changed
        id: commit
        env:
          SIGNING_KEY: ${{ secrets.PUBLIC_SYNC_SIGNING_KEY }}
        run: |
          set -euo pipefail

          if git diff --quiet -- public/schedule-index.json; then
            echo "Index unchanged — nothing to commit."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # No unsigned fallback. public-sync.yaml has one because it serves
          # repos that have not been onboarded yet; here the secret is known to
          # exist, and quietly pushing unsigned is the exact thing this step is
          # here to stop.
          if [ -z "$SIGNING_KEY" ]; then
            echo "::error::PUBLIC_SYNC_SIGNING_KEY is empty -- refusing to push unsigned."
            exit 1
          fi

          # Mode before contents, so the key is never briefly world-readable.
          # RUNNER_TEMP rather than the working directory: the repo is checked
          # out at the workspace root here, and a private key inside the tree is
          # one stray `git add -A` away from being committed.
          install -m 600 /dev/null "$RUNNER_TEMP/signing-key"
          printf '%s\n' "$SIGNING_KEY" > "$RUNNER_TEMP/signing-key"

          git config gpg.format ssh
          git config user.signingkey "$RUNNER_TEMP/signing-key"
          git config commit.gpgsign true

          # Author and committer both, byte-identical: GitHub matches the
          # signature to the account by committer email, and collapses the two
          # into one line on the commit list only when the strings match.
          git config user.name "Jackson Wearn"
          git config user.email "jcwearn@users.noreply.github.com"

          git add public/schedule-index.json
          git commit -m "Refresh schedule index"

          # commit.gpgsign does not cover the commits a rebase RE-creates, and
          # the loop below rebases. Without this, one push race turns a signed
          # commit into an unsigned one.
          git config rebase.gpgSign true

          # main moves under this job: a PR merge, another dispatch, the
          # exporter's "run both" button. The push was previously a single
          # attempt, so any of those rejected it, failed the job, and dropped
          # that day's index on the floor -- the site kept serving the old one
          # behind a red run nobody watches.
          for attempt in 1 2 3; do
            # Not `git verify-commit`: verifying an SSH signature locally needs
            # a gpg.ssh.allowedSignersFile the runner does not have, so it
            # would fail on a perfectly good signature. Checking the header is
            # attached is what can be established here; GitHub verifies it
            # server-side.
            #
            # Inside the loop, not before it: a rebase re-creates the commit,
            # so a check that ran only once would not be checking the commit
            # that actually gets pushed.
            if ! git cat-file commit HEAD | grep -q '^gpgsig'; then
              echo "::error::Signing was configured but HEAD carries no signature."
              exit 1
            fi
            echo "Commit is signed."

            if git push; then break; fi

            if [ "$attempt" = 3 ]; then
              echo "::error::main kept moving under us; gave up after 3 pushes."
              exit 1
            fi

            echo "Push rejected -- main moved. Rebasing and retrying."
            git fetch origin main
            # FETCH_HEAD, not origin/main: a fetch with an explicit refspec
            # only updates the remote-tracking ref opportunistically, and the
            # one ref it is guaranteed to write is this one.
            git rebase FETCH_HEAD
          done

          # Read AFTER the push, and after any rebase. A rebase rewrites the
          # commit, so a SHA captured before this point names an object the
          # deploy job would fail to check out.
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Remove signing key
        if: always()
        run: rm -f "$RUNNER_TEMP/signing-key"

  # Publishes the index the job above committed.
  #
  # Calling the deploy workflow directly needs no extra credential. Pushing with
  # a PAT or a GitHub App token so `on: push` fires would buy the same outcome
  # for the cost of another long-lived secret.
  #
  # `ref` is not optional here, whatever the input says. A reusable workflow
  # inherits the CALLER's github context, so github.sha inside it is the head
  # this run started at -- the commit BEFORE the refresh above. Without `ref`
  # it checked that out, built its public/schedule-index.json, and shipped it:
  # every run published the index the previous run had generated. It went green
  # every time, and the only thing that ever caught the site up was an
  # unrelated merge to main firing deploy.yml against the real head.
  deploy:
    name: Publish the refreshed schedule
    needs: refresh
    if: needs.refresh.outputs.changed == 'true'
    permissions:
      contents: read
      pull-requests: write
    uses: jcwearn/workflows/.github/workflows/cloudflare-pages-deploy.yaml@d88dd60ddaeeb65b628758a975492fb6a4f67b89 # v1
    with:
      project-name: anupama-and-jackson
      timeout-minutes: 15
      ref: ${{ needs.refresh.outputs.sha }}
    secrets:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
